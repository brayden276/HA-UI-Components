{
  "config": {
    "abort": {
      "already_configured": "Split State Registry is already configured."
    },
    "step": {
      "user": {
        "title": "Split State Registry",
        "description": "Create the shared split-system state registry."
      }
    }
  }
}
