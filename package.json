{
  "name": "ha-component-library",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HACS Dashboard bundle for the reusable cards from the Home Assistant Components dashboard.",
  "scripts": {
    "bundle": "node scripts/assemble.mjs",
    "check": "node scripts/check-all.mjs"
  }
}
