{
  "config": {
    "step": {
      "user": {
        "title": "Split State Registry",
        "description": "Create the local persistent registry used by the Split System Components.",
        "data": {}
      }
    },
    "abort": {
      "already_configured": "Split State Registry is already configured."
    }
  }
}
